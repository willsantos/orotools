// Package requirements checks external tool requirements declared by recipes
// (command presence and version constraints). Reused by the new/init/apply and
// doctor commands (spec section 14): one detection engine, no duplication.
package requirements
